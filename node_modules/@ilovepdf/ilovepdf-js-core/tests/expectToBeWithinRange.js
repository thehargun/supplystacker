"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const globals_1 = require("@jest/globals");
globals_1.expect.extend({
    toBeWithinRange(actual, expected, errorMargin) {
        const { isNot } = this;
        const printExpected = this.utils.printExpected;
        const floor = expected - errorMargin;
        const ceiling = expected + errorMargin;
        const pass = actual >= floor && actual <= ceiling;
        return {
            pass,
            message: () => `expected ${this.utils.printReceived(actual)} ${isNot ? 'not ' : ''}to be within range ${printExpected(`${floor} - ${ceiling}`)} (expected: ${printExpected(expected)}, errorMargin: ${printExpected(errorMargin)})`,
        };
    },
});
//# sourceMappingURL=expectToBeWithinRange.js.map