<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Products</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 15px;
        }
        
        h1 {
            text-align: center;
            margin-bottom: 20px;
            font-size: 24px;
        }
        
        .product-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-between;
            max-width: 100%;
            margin: 0 auto;
            gap: 15px;
        }
        
        .product {
            border: 1px solid #ddd;
            padding: 8px;
            box-sizing: border-box;
            width: calc(25% - 12px);
            text-align: center;
            page-break-inside: avoid;
            height: 280px;
            display: flex;
            flex-direction: column;
        }

        .product img {
            width: 100%;
            height: 180px;
            object-fit: contain;
            margin-bottom: 8px;
        }

        .product-details {
            flex-grow: 1;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }
        
        .product-details h3 {
            margin: 5px 0;
            font-size: 11px;
            line-height: 1.2;
            height: 40px;
            overflow: hidden;
            display: -webkit-box;
            -webkit-line-clamp: 3;
            -webkit-box-orient: vertical;
        }
        
        .product-details .price {
            margin: 5px 0 0 0;
            font-size: 14px;
            font-weight: bold;
            color: #000;
        }

        .product:nth-child(12n) {
            page-break-after: always;
        }

        @page {
            size: A4;
            margin: 15mm;
        }

        @media print {
            body {
                padding: 0;
            }
            
            .product-container {
                gap: 10px;
            }
            
            .product {
                width: calc(25% - 8px);
                height: 260px;
            }
        }
    </style>
</head>
<body>
    <h1>Products</h1>

    <div class="product-container">
        <% inventory.forEach((item, index) => { %>
            <div class="product">
                <img src="<%= item.imageUrl %>" alt="<%= item.itemName %>">
                <div class="product-details">
                    <h3><%= item.itemName %></h3>
                    <div class="price">$<%= item[`priceLevel${req.session.user.priceLevel}`] ? item[`priceLevel${req.session.user.priceLevel}`].toFixed(2) : 'N/A' %></div>
                </div>
            </div>
        <% }); %>
    </div>
</body>
</html>