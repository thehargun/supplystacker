"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const ElementAlreadyExistsError_1 = __importDefault(require("../../../errors/ElementAlreadyExistsError"));
class Receiver {
    constructor(name, email, params = {}) {
        this.name = name;
        this.email = email;
        this.params = params;
        this.files = [];
    }
    addFile(file) {
        const index = this.files.indexOf(file);
        if (index !== -1)
            throw new ElementAlreadyExistsError_1.default();
        this.files.push(file);
    }
    toJSON() {
        const files = this.files.map(file => file.toJSON());
        return Object.assign({ name: this.name, email: this.email, files }, this.params);
    }
}
exports.default = Receiver;
//# sourceMappingURL=Receiver.js.map