import ILovePDFTool from "../types/ILovePDFTool";
import { TaskParams } from './Task';
import Auth from "../auth/Auth";
import XHRInterface from "../utils/XHRInterface";
import TaskBaseProcess, { ProcessParams } from "./TaskBaseProcess";
interface PdfJpgProcessParams extends ProcessParams {
    pdfjpg_mode?: 'pages' | 'extract';
    dpi?: number;
}
export default class PdfJpgTask extends TaskBaseProcess {
    type: ILovePDFTool;
    constructor(auth: Auth, xhr: XHRInterface, params?: TaskParams);
    /**
     * @inheritdoc
     * @override
     * @param params - ProcessParams object with extra attrs for this service.
     */
    process(params?: PdfJpgProcessParams): Promise<import("./TaskBaseProcess").TaskBaseProcessProcess>;
}
export {};
